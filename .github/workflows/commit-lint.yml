name: Commit lint

# Thin caller — delegates commit-message validation to the shared
# reusable workflow. Logic lives in
# akira-toriyama/.github/.github/workflows/commit-lint.yml.

on:
  pull_request:

jobs:
  lint:
    uses: akira-toriyama/.github/.github/workflows/commit-lint.yml@main
